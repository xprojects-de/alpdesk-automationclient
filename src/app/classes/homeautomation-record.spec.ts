import { HomeautomationRecord } from './homeautomation-record';

describe('HomeautomationRecord', () => {
  it('should create an instance', () => {
    expect(new HomeautomationRecord()).toBeTruthy();
  });
});
