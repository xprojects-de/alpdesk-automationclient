import { HomeautomationParam } from './homeautomation-param';

describe('HomeautomationParam', () => {
  it('should create an instance', () => {
    expect(new HomeautomationParam()).toBeTruthy();
  });
});
