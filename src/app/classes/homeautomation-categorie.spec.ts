import { HomeautomationCategorie } from './homeautomation-categorie';

describe('HomeautomationCategorie', () => {
  it('should create an instance', () => {
    expect(new HomeautomationCategorie()).toBeTruthy();
  });
});
